USE POSTMAN

### 1. GET articles.index:
GET localhost:3000/api/v1/articles

### 2. POST new article:
POST localhost:3000/api/v1/articles
USE JSON format to post.
```JSON
{
    "article":"YOUR_ARTICLE",
    "body":"YOUR_TEXT"
}
```